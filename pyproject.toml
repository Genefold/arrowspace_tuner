[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "arrowspace_tuner"
version = "0.5.0"
description = "Hyperparameter discovery (eps auto-tuning) for ArrowSpace via Optuna"
readme = "README.md"
license = { text = "Apache-2.0" }
authors = [
    { name = "Tommaso Moriondo", email = "moriondotommaso@gmail.com" },
]
requires-python = ">=3.12"
keywords = [
    "vector-search",
    "spectral-analysis",
    "hyperparameter-tuning",
    "optuna",
    "arrowspace",
    "graph-laplacian",
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Intended Audience :: Science/Research",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Typing :: Typed",
]

# ── Hard dependencies ──────────────────────────────────────────────────────
# These are always required: the Rust wheel, Optuna, SciPy for Fiedler, NumPy.
# plotly and pandas are NOT here — they are opt-in via [report].
# arrowspace support range: 0.26.x–0.28.x tested (see README Requirements);
# the upper bound is the tested ceiling, not a known incompatibility.
dependencies = [
    "arrowspace>=0.26.0,<0.29",
    "click>=8.1",
    "numpy>=2.4.4",
    "optuna>=4.8.0",
    "scipy>=1.17.1",
]

[project.scripts]
arrowspace-tuner = "arrowspace_tuner.cli:main"

# ── Optional extras ─────────────────────────────────────────────────────────
[project.optional-dependencies]

# pip install arrowspace_tuner[report]
# Needed for tuner.save_report() and all HTML/CSV output from reporting.py
report = [
    "plotly>=6.7.0",
    "pandas>=3.0.0",
]

# pip install arrowspace_tuner[mcp]
# Optional local stdio MCP server for LLM clients (arrowspace-tuner mcp).
# The base package never depends on MCP.
mcp = [
    "mcp>=1.0.0",
]

# pip install arrowspace_tuner[dev]
# Full dev environment: testing + linting + type checking
dev = [
    "pytest>=8.0",
    "pytest-cov>=5.0",
    "ruff>=0.9",
    "mypy>=1.15",
    "mcp>=1.0.0",     # needed to test mcp_server.py
    "plotly>=6.7.0",   # needed to test reporting.py
    "pandas>=3.0.0",
]

[project.urls]
Homepage = "https://github.com/Genefold/arrowspace_tuner"
Repository = "https://github.com/Genefold/arrowspace_tuner.git"

# ── Hatchling config ───────────────────────────────────────────────────────────
[tool.hatch.build.targets.wheel]
packages = ["src/arrowspace_tuner"]
exclude = [
    "tests/",
    "notebooks/",
    "docs/",
    ".github/",
    "*.db",
    "*.sqlite",
    "*.ipynb",
    ".ruff_cache/",
    ".mypy_cache/",
    ".pytest_cache/",
    "dist/",
    "*.egg-info/",
]

# ── Ruff ──────────────────────────────────────────────────────────────────
[tool.ruff]
line-length = 100
target-version = "py312"
# notebooks and one-off scripts are not part of the linted package surface
extend-exclude = ["notebooks", "scripts"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "ANN"]
ignore = ["ANN101"]

# ── Mypy ────────────────────────────────────────────────────────────────────
[tool.mypy]
python_version = "3.12"
strict = true
ignore_missing_imports = true   # arrowspace has no stubs

# ── Pytest ─────────────────────────────────────────────────────────────────
[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "--cov=arrowspace_tuner --cov-report=term-missing"

[dependency-groups]
dev = [
    "twine>=6.2.0",
]
